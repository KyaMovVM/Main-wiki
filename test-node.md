# Спецификация для `src/test-node.js`

## Что будет делаться

Мы подготовим два файла, которые позволят запустить ваш Node‑скрипт **src/test-node.js** в Docker‑контейнере и автоматически развернуть его через CI/CD‑pipeline Gitea. Всё написано простым языком для Junior‑разработчика.

---

## 1️⃣ Dockerfile – образ контейнера

```dockerfile Dockerfile
# Базовый лёгкий образ Node.js (≈30 МБ)
FROM node:20-alpine AS builder

# Рабочая директория внутри контейнера
WORKDIR /app

# Копируем только нужный файл (можно скопировать весь проект, если хотите)
COPY src/test-node.js ./test-node.js

# Открываем порт, на котором будет слушать сервер
EXPOSE 3010

# Команда, которая выполнится при старте контейнера
CMD ["node", "test-node.js"]
```

### Что делает каждый шаг  

| Шаг | Описание |
|-----|----------|
| `FROM node:20-alpine` | Берём минимальный образ Node.js на базе Alpine Linux. |
| `WORKDIR /app` | Устанавливаем рабочую директорию — все последующие команды будут выполняться здесь. |
| `COPY src/test-node.js ./test-node.js` | Копируем ваш скрипт в контейнер. |
| `EXPOSE 3010` | Объявляем, что контейнер будет слушать порт 3010 (это нужно для проброса порта наружу). |
| `CMD ["node", "test-node.js"]` | При запуске контейнера сразу стартует ваш Node‑сервер. |

### Как собрать и запустить локально

```bash
# Сборка образа, тегируем как test-node
docker build -t test-node .

# Запуск в фоне, пробрасываем порт 3010 наружу
docker run -d -p 3010:3010 --name test-node-container test-node
```

После этого ваш сервер будет доступен по адресу `http://localhost:3010`.

---

## 2️⃣ Gitea Actions workflow – автоматическая сборка и запуск

Создайте каталог **`.gitea/workflows`** (если его ещё нет) и добавьте файл **`docker-deploy.yml`**:

```yaml .gitea/workflows/docker-deploy.yml
name: Build & Deploy test-node
on:
  push:
    branches: [ main ]          # запуск при пуше в ветку main
  pull_request:
    branches: [ main ]

jobs:
  build-and-run:
    runs-on: self-hosted        # используем ваш собственный runner с Docker
    timeout-minutes: 15         # ограничиваем выполнение 15‑ю минутами

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Docker Buildx (optional, for multi‑arch)
        uses: docker/setup-buildx-action@v2

      # Если нужно пушить образ в реестр – раскомментируйте и заполните параметры
      # - name: Log in to Docker registry
      #   uses: docker/login-action@v2
      #   with:
      #     registry: ghcr.io
      #     username: ${{ secrets.REGISTRY_USER }}
      #     password: ${{ secrets.REGISTRY_PASSWORD }}

      - name: Build Docker image
        run: |
          docker build -t test-node:${{ github.sha }} .

      - name: Run container (detached)
        run: |
          docker run -d --rm \
            -p 3010:3010 \
            --name test-node-${{ github.run_id }} \
            test-node:${{ github.sha }}

      # Простейшая проверка, что сервер действительно запустился
      - name: Verify service is reachable
        run: |
          sleep 5   # даём Node немного времени на старт
          curl -f http://localhost:3010 || exit 1

      # Остановим контейнер после завершения работы (чистка)
      - name: Stop container
        if: always()
        run: |
          docker rm -f test-node-${{ github.run_id }} || true
```

### Ключевые моменты

- **`runs-on: self-hosted`** – workflow будет исполняться на вашем собственном раннере (тот, о котором вы упоминали). На нём должен быть установлен Docker.
- **`timeout-minutes: 15`** – гарантирует, что весь job завершится не позже 15 минут, как требовалось в задаче «развернуть на 15 минут».
- После сборки образа контейнер запускается в фоне, делается короткая health‑check (`curl`) и в конце контейнер останавливается, чтобы runner оставался чистым.

---

## 📋 Как начать работать

1. **Добавьте файлы**  
   - `Dockerfile` в корень репозитория.  
   - `.gitea/workflows/docker-deploy.yml` (создайте каталог, если его нет).

2. **Настройте Gitea‑раннер** на машине с Docker.

3. **Сделайте коммит и пуш** в ветку `main`. Workflow автоматически запустится, соберёт образ, поднимет контейнер на 15 минут и потом остановит его.

---

## Что будет видно Junior‑разработчику

- Как собрать минимальный Docker‑образ для Node‑приложения.  
- Как пробросить порт из контейнера наружу.  
- Как написать простой CI/CD‑скрипт в Gitea, который ограничивает время выполнения задачи.  
- Как проверять, что сервис действительно работает (curl + небольшая пауза).  

Если всё сделано правильно, после пуша вы увидите в разделе **Actions** статус `success`, а ваш сервер будет доступен по `http://<runner‑host>:3010` ровно столько времени, сколько позволил таймаут.

Удачной работы! 🚀